# 视频编辑任务定义

## 1. 任务概述

该任务用于对一段已有视频进行局部内容编辑。

用户提供待编辑视频、参考图、编辑提示词和目标区域 `bbox` 坐标，系统输出编辑后的视频文件。

这个任务的核心目标是：

- 保持原视频的整体时序、运镜和未编辑区域尽量不变
- 仅在指定区域内，根据参考图和提示词完成内容替换或重绘
- 生成一个可直接交付或继续后处理的编辑结果视频

## 2. 输入定义

一个视频编辑任务最少包含以下输入：

| 字段名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `video_path` | `string` | 是 | 需要编辑的视频文件路径 |
| `reference_image_path` | `string` | 是 | 参考图路径，用于指导目标区域的视觉结果 |
| `prompt` | `string` | 是 | 编辑提示词，描述希望替换成什么内容 |
| `bbox` | `array[number]` | 是 | 需要编辑区域的坐标，建议格式为 `[x1, y1, x2, y2]` |

## 3. bbox 说明

`bbox` 表示用户在视频画面中框选出的编辑区域，建议统一为：

```json
[x1, y1, x2, y2]
```

约定如下：

- `x1`, `y1`：左上角坐标
- `x2`, `y2`：右下角坐标
- 坐标原点为视频帧左上角
- 坐标单位为像素
- 要求满足 `0 <= x1 < x2` 且 `0 <= y1 < y2`

如果后端后续需要更强可读性，也可以在接口层扩展为对象格式：

```json
{
  "x1": 120,
  "y1": 80,
  "x2": 420,
  "y2": 560
}
```

## 4. 输出定义

任务输出为编辑完成后的视频文件路径。

| 字段名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `output_video_path` | `string` | 是 | 编辑后生成的视频路径 |

## 5. 业务流程定义

该任务可以简单抽象为：

1. 读取原始视频
2. 读取参考图
3. 解析提示词
4. 根据 `bbox` 确定编辑区域
5. 在保持其余区域和原视频动态尽量一致的前提下，完成局部编辑
6. 输出最终编辑视频

## 6. 建议的任务语义

从后端接口设计角度，可以把这个能力定义为：

`输入一个原视频，在指定区域内，参考参考图和提示词进行局部编辑，输出编辑后的视频。`

更标准一点的描述可以写成：

`Video Editing = f(video_path, reference_image_path, prompt, bbox) -> output_video_path`

## 7. 示例

### 请求语义示例

```json
{
  "video_path": "/data/input/demo.mp4",
  "reference_image_path": "/data/reference/target.png",
  "prompt": "将框选区域中的人物衣服替换为白色西装，保持其他内容不变",
  "bbox": [120, 80, 420, 560]
}
```

### 返回语义示例

```json
{
  "output_video_path": "/data/output/demo_edited.mp4"
}
```

## 8. 后续 API 设计建议

为了后续接口文档更稳定，建议尽早统一以下几点：

- `bbox` 使用绝对像素坐标还是归一化坐标
- `reference_image_path` 是否允许为空
- 输入是本地路径、上传文件，还是两种方式都支持
- 输出是直接返回文件路径，还是返回任务 ID 后异步查询结果
- 是否需要增加可选参数，例如分辨率、帧数、编辑强度、随机种子等

## 9. 一句话定义

这是一个“基于参考图和文本提示、对视频指定区域进行局部编辑并输出结果视频”的任务。
